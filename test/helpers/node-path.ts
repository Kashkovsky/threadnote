/** Test-only boundary for synchronous path construction in fixtures. */
import path from 'node:path';

export const {basename, delimiter, dirname, isAbsolute, join, posix, relative, resolve, sep, win32} = path;
