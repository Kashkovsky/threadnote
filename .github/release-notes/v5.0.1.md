## What's new

Threadnote 5.0.1 keeps code-graph storage ready when scoped cleanup records are present, avoiding unnecessary reinitialization.

### Scoped cleanup records keep the graph ready

Code-graph storage now recognizes valid scoped cleanup cursors during startup. Existing repositories can continue using a
ready graph instead of unnecessarily replaying schema initialization after scoped cleanup.
