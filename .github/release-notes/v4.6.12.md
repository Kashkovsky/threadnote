## What's new

Threadnote 4.6.12 updates bundled dependencies to resolve nine reported security vulnerabilities in request parsing and URI handling.

### Security updates

The release includes patched versions of `qs`, `fast-uri`, and `hono`. No user action is needed beyond updating Threadnote.
