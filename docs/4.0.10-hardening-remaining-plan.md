# Threadnote 4.0.10 remaining hardening plan

Updated: 2026-08-10. This is the active execution checklist. The larger
`docs/4.0.10-dogfood-hardening-plan.md` remains the historical rationale, evidence, and safety record; do not use it
as the day-to-day tracker.

## Working agreement

- Keep changes in reviewable milestone commits on PR #131.
- Run focused local checks only. After each runtime milestone: commit a clean HEAD, install it globally with
  `bun run dev:install-global`, smoke the affected flow through the installed `threadnote`, push, and let CI run the
  broad validation.
- Continue the next local milestone while CI runs; inspect it periodically and fix evidence-backed failures.
- Preserve all destructive-action, privacy, lease, writer-gate, and prior-ready-snapshot invariants from the historical
  plan.
- Keep new or extracted implementation files below 1,000 lines. Split by responsibility before a module crosses that
  boundary.

## Landed baseline — do not reopen without new evidence

- Phases A-D are complete.
- Phase F selected view and isolated snapshot deletion is complete through `9194ef5`.
- Phase G runtime-skew reconnect handling is complete through `fd6f847`.
- Phase H1 inventory preview, low-signal admission, parser budgets, committed structured-blob reuse, and retired cache
  reclamation are complete through `df100bd`, `9c0c363`, `33ab99d`, `f9298e1`, and `e04005e`.
- Bounded project-closure reattribution, storage attribution, extraction telemetry, abandoned-status pruning, fresh-cache
  protection, and cache-generation isolation are complete through `76ed256`, `42f3854`, `d9125d1`, `e655eb9`,
  `e04005e`, and `0236a5b`.
- Store contracts/models have been extracted in `c674728`. Attach/cleanup contention was stabilized, globally
  dogfooded, and pushed in `3035b44`.

## M0 — close the current CI stabilization slice

- [x] Make PR #131 required CI green from `3035b44` or its evidence-backed follow-ups.
- [x] Confirm shared-ready attach, promotion resume, incremental properties, and heavy integration no longer lose time
      to duplicate cleanup sessions or zero-wait writer races.

## M1 — Manager graph administration cleanup

- [x] Replace the large `Preview remove` and `Remove view` controls with one accessible inline remove-icon button per
      removable view; keep preview-bound approval in the resulting dialog.
- [x] Remove the duplicate folder label beside the indexed-view selector; retain folder/provenance inside the selected
      view and administration details.
- [x] Replace the growing completed build/status list with bounded, user-relevant current/recent state and automatic
      pruning. Explain active, queued, failed, or recovery-relevant entries; omit repetitive completed rows.
- [x] Make `Query the code graph` full-width and preserve usable input/action sizing.
- [x] Verify the administration layout and interactions in the browser at representative desktop and mobile widths.

M1 browser evidence: the exact installed milestone was exercised at 1,440 × 900 and 390 × 844. The open Manager
reconciled a removed linked worktree from two views to one on its next status poll (about 1.3 seconds) without a page
reload. Administration cards now constrain long worktree paths and action rows to the mobile viewport, keeping the
inline remove control reachable, while the graph query occupies the full toolbar row.

## M2 — finish Store decomposition

- [x] Extract public models and service contracts below 1,000 lines.
- [x] Extract schema/version/migration helpers into bounded modules.
- [x] Extract writer gates, sessions, leases, and routine cleanup coordination into bounded modules.
- [x] Extract activation, persistent-build, resolution, and cache/materialized-shard domains into bounded modules.
- [x] Extract view reconciliation, selected purge, retirement, diagnostics, query, analysis, and visualization domains
      into bounded modules.
- [x] Leave `store.ts` as a below-1,000-line service assembly/barrel with a stable public API and no dependency cycles.

## M3 — finish Phase E lifecycle convergence and live disk safety

- [x] Complete one typed lifecycle classification for missing views, abandoned builds, unreadable/corrupt stores,
      orphaned sidecars, retired generations, and still-required shared clean bases.
- [x] Run bounded provenance reconciliation at startup, catalog/diagnostics, index completion, and critical graph-error
      opportunities, not only idle maintenance.
- [x] Automatically reclaim only authority-proven disposable state while preserving active writers, leases, pins,
      required bases, and live sibling consumers.
- [x] Finish free-space resampling and reservation coverage for all remaining large write surfaces; stop at safe resumable
      boundaries and preserve the prior ready snapshot.
- [x] Add pressure-aware reclaim cadence with separate logical-row, reclaimable-page, WAL/TEMP, and actual filesystem
      byte reporting.
- [x] Prove lifecycle containment, idempotence, and convergence with bounded generated properties plus missing-folder,
      corruption, active-writer, shared-base, low-disk, and multi-worktree regressions.

M3 evidence: foreground lifecycle opportunities run one non-joining, non-tailing unit and resolve Git identity only after
a missing candidate is observed. The long-lived Manager status stream advances one bounded unit while idle and switches
to its active cadence while reconciliation is pending, so cross-process catalog revisions remove stale views without a
reload.
TEMP and durable write reservations now cover inventory, workspace, fact, incremental, resolution, and publication
boundaries; generated properties cover lifecycle convergence, protection dominance, pressure monotonicity, and
filesystem separation. Focused M3 checks passed 171 unit/property tests, eight destructive/resumability regressions,
the exact previously timing-out promotion regression, and source/test typechecks.

## M4 — finish Phase H2 incremental and resumable rebuilds

- [x] Recover compatible peer-independent work without mixing final shards whose cross-file attribution can be stale;
      one invalid cache item must not discard unrelated valid work.
- [x] Reuse the nearest compatible clean snapshot and content-addressed rows with conservative reverse-dependent
      invalidation; ambiguous evidence falls back to a full closure.
- [x] Preserve a completed stale commit as queryable while atomically queuing the latest target across linked worktrees.
- [x] Extend resumable checkpoints across remaining inventory, extraction, resolution, and activation boundaries.
- [x] Gate one-file and nearby-commit work on changed closure and rewritten bytes/rows, with measured improvement on the
      audited host.

M4 evidence: parser-cache admission repairs only an invalid peer while final attributed shards remain all-or-nothing;
bounded Git ancestry now selects the unique nearest reusable clean commit, with equal-distance merge parents failing
closed. The indexer reports and gates changed files, deleted files, source bytes, final fact bytes, and planned staging
rows independently of unrelated repository size. A stale post-activation snapshot remains ready/queryable while the
bounded retry builds the newest worktree state, and watcher signals collapse intermediate targets into one latest
trailing run. Existing persistent inventory/materialization, resolution, and activation checkpoints were rerun across
process/session boundaries. M4 also closes the M3 CI evidence gaps by routing index completion through one direct Store
cleanup unit and making lifecycle/session tests deterministic around the intentional bounded maintenance work.

## M5 — finish Phase H3/H4 compactness and throughput

- [x] Complete per-table/index/snapshot byte attribution and explain the production bytes-per-symbol baseline.
- [x] Measure and implement compact lexical/fact storage plus deterministic snapshot retention caps without weakening
      query-quality gates.
- [x] Serve structural-ready before optional vectors/maintenance and minimize the checkout-exclusive publication window.
- [x] Remove duplicate passes, bound each resolution pass, and evaluate cost-aware extraction plus SQLite bulk/shadow
      strategies from measurements rather than timeout increases.

M5 measured the exact source database at 16.85 GiB: 13.10 GiB was attributed to live B-trees, including 11.40 GiB of
shared/historical fact-cache objects, while the unique active snapshot carried only 210.6 MiB of logical payload. That
separates the 336,438 attributed physical bytes/symbol baseline from the 5,281 active logical bytes/symbol payload and
also exposes bounded per-snapshot raw/stored fact totals. The deterministic level-3 fact envelope reduced the active
109.95 MB raw corpus to about 16.7 MB including envelopes (84.8%) at roughly 65 MiB/s encode and 95 MiB/s decode;
legacy rows remain readable and corrupt envelopes fail closed. Routine maintenance now retains at most two unprotected
detached-ready snapshots and an estimated 4 GiB per repository, coalesces equivalent graphs, pages retirement at 16,
and protects active views, live leases, and required bases.

Pointer promotion now emits structural-ready before optional analysis/vector work. Extraction lanes isolate >=512 KiB
files and >=128 KiB structured files, reference resolution has a 32-pass convergence fence while every pass remains
page-bounded, and the existing pipeline performs one bounded extraction/materialization traversal. A 73,000-row load
kept compact file/shard transaction boundaries below 118/165 KiB, WAL below 4.3/8.7 MB, and reported RSS below
643/725 MB in
9.8 seconds. Those measurements support keeping the serialized/backpressured direct writer, current WAL checkpoint,
and secondary-index strategy; no shadow database, timeout increase, or speculative SQLite pragma change was justified.

## M6 — finish Phase H5/H6 observability and Nx architecture

- [x] Finish phase ETA and queue/latest-target/stale-ready reporting from source bytes, language/size class, emitted
      facts, and observed duration; keep all telemetry path-free.
- [x] Keep Manager/catalog/query/status partial and bounded during production-shaped multi-checkout contention.
- [x] Persist typed Nx/pnpm/tsconfig components and declared dependencies with stable IDs, provenance, source roots, and
      diagnostics; build manifest maps once and avoid package-count-quadratic scans.
- [x] Precompute exact cross-component aggregates while keeping declared build dependencies distinct from resolved
      source references and preserving manifest-declared active source under vendor-like directory names.

M6 now plans extraction once with deterministic language/size-class work units, streams source and emitted-fact bytes,
and bases ETA on observed class-weighted progress before falling back to byte/file measures. Existing latest-target,
stale-ready, bounded catalog/query/status, finite-lease, and production-shaped contention contracts remain covered; the
Manager also retains a pending Administration refresh until the exact changed catalog revision is diagnosed
successfully, so a transient maintenance response cannot leave removed views visible until reload.

Workspace discovery now materializes typed Nx projects/targets, pnpm packages, and tsconfig projects/references with
stable identities and declared dependency provenance. Package and manifest maps are parsed once, ancestor lookup is
path-depth bounded, and permutation properties cover deterministic IDs and relationships. The split keeps each new
workspace implementation file below 1,000 lines. Persistent extension revision 9 stores receipt-validated exact
cross-component source aggregates after structural readiness; declared build dependencies remain separate, and old
large snapshots without receipts return an explicit partial result instead of running an unbounded Manager GROUP BY.
Exact-HEAD upgrade dogfooding also covers the additive revision-8-to-9 path: verified cleanup authority survives the
schema extension, and component aggregation materializes an indexed temporary scope map before an edge-first scan.
The production-shaped 42,177-symbol/127,327-edge run exposed and removed the prior billions-of-comparisons join plan;
an EXPLAIN regression requires two primary-key endpoint lookups and forbids a scope-table scan.

## M7 — finish remaining Phase G feature programs

- [x] Add measured Android/iOS resource relevance, package-local absence hints, and implementation/side-effect-owner
      ranking over lexical test/documentation matches.
- [x] Design bounded workset-aware cross-repository graph queries with explicit repository provenance.
- [x] Clarify agent guidance for honest known-path, remote-review, and asset-only graph skips while retaining graph-first
      investigation for unfamiliar local source.

M7 advances extractor generation 12. Android `res` XML now retains bounded elements, attributes, and explicit
`@type/name` wiring; Apple plist/storyboard/XIB resources and `.xcassets` scalar values retain searchable identifiers
without claiming pixel or visual interpretation. Resource evidence carries the nearest typed component, exact package
queries return bounded examined/matched counts and label zero matches as a package-local hint, and both lexical and
semantic ranking prefer implementation-side mutation owners over test/documentation copies unless those artifacts are
requested explicitly. Named worksets inspect at most eight existing ready repository snapshots with deterministic
24-node/40-edge shared budgets, two-repository concurrency, per-member repository/snapshot provenance, explicit
unavailable coverage, and no cold-build fan-out. Canonical packaged, contributor, and Cursor guidance now records the
bounded known-path, no-checkout remote-review, and visual/binary skip cases. The Manager also projects confirmed
view removal into both catalogs immediately and idempotently before and after its authoritative refresh, closing the
remaining reload-only stale-count window. Focused M7 evidence covers 71 resource/query/ranking/workset/removal tests,
100-run removal and owner-ranking properties, a 150-run fair workset allocation property, CLI/MCP/package guidance,
the prior schema/reconciliation CI regressions, the unresolved-project closure fallback, typecheck, and changed-file
lint; full validation remains delegated to CI as requested.

## M8 — release evidence and closeout

- [x] Capture exact-current same-machine cold, one-file dirty, nearby clean, interrupted/resumed, concurrent-worktree,
      and read-during-writer baselines with per-phase CPU/time/RSS/bytes attribution.
- [x] Maintain the public heavy-tail/multi-worktree fixture and private exact-shape evidence; trend correctness,
      responsiveness, storage, and recovery without extending timeouts to hide regressions.
- [x] Require no active-source omission, privacy leak, unbounded writer wait, abandoned live owner, cross-worktree
      corruption, or unsafe low-disk behavior before making maximum-performance claims.
- [x] Update the historical plan, website/operator docs, release evidence, and active handoff with final commits, CI
      evidence, installed-runtime smoke, remaining deferrals, and an honest completion percentage.
- [ ] Publish the privacy-safe closeout to the shared durable dogfood issue ledger after the user explicitly confirms
      that publication. This policy-gated memory action is not source implementation or a release correctness gate.

M8 binds the milestone-series correctness and same-machine comparisons to a versioned public fixture, focused
properties, required PR checks, a clean installed runtime, and separate exact-tag evidence. The first strict public
production-large observation on `3b7c52c` reached its 20-minute measured boundary during materialization and retained
its sampler/checkpoint artifact instead of extending the timeout. That result withholds a maximum-performance claim; it
does not invalidate the containment, recovery, incremental-parity, or bounded-read gates already measured in their
separate lanes. The stale vector-score, Windows temporary-cleanup, MCP completion-log, current-schema no-op, MCP payload,
and heavy-tail admission contracts exposed by the first CI/evidence runs are corrected by `46fb831`. M3–M8 source
implementation is 100% complete; exact-HEAD installation is recorded in the release evidence and active handoff, while final required CI,
handoff/issue-ledger closeout, merge, and tag publication remain release operations rather than hidden implementation
work.

Final closeout follow-ups keep platform evidence honest instead of repeatedly weakening one cross-platform number:
Windows development-fixture headroom is selected from recorded runtime provenance, while Linux/macOS, vector, scale,
quality, and safety budgets retain their reviewed limits. Evidence-only directory telemetry tolerates atomic removal of
temporary vector metadata, and the completed eight-process Windows CLI logging case gets a bounded lock-queue budget so
both privacy-safe lifecycle entries survive. The 10k pinned-vector rerun passed after the telemetry fix. The active
handoff carries the exact containing commit and final required-CI result; the shared durable ledger remains deliberately
unpublished until the explicit confirmation required by repository policy.
