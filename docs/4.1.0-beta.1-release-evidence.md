# Threadnote 4.1.0 beta.1 release evidence

This is the concise release-facing evidence record for the hardening work tracked in
[`4.0.10-hardening-remaining-plan.md`](./4.0.10-hardening-remaining-plan.md). The historical investigation remains in
[`4.0.10-dogfood-hardening-plan.md`](./4.0.10-dogfood-hardening-plan.md).

## Candidate provenance

- Pre-closeout implementation candidate: `57fe56436ab40cabadaa73992f54ce9dd5eea6e1` on PR #131. The final PR head adds
  only the evidence-backed Windows log-lock and platform-budget closeout described below.
- Exact installed dogfood runtime: `4.1.0-beta.1.local.g57fe56436ab40cabadaa73992f54ce9dd5eea6e1`, executable SHA-256
  `dc247936f25656a5e559fa710a83a2ab6646a81e4a3a178c6adfc22ea5cacafa`.
- Exact-HEAD smoke: the installed binary indexed clean commit `57fe564` as snapshot
  `cgsn_4be2f300a4bec966dbc5dabe95376bac97a70341` with 732 files, 42,406 symbols, and 128,022 edges. A structured query
  parsed successfully and diagnostics reported one view, two retained ready snapshots, and zero active builds. The
  active handoff records the final containing commit and required-CI run rather than claiming that a document can
  identify its own containing commit.
- Final release source is the clean merge commit tagged `v4.1.0-beta.1`; the tag workflow independently verifies that
  package version, release notes, tag, checkout, artifacts, and retained release-evidence provenance agree.

## Evidence lanes

The evidence is intentionally split by workload. Timings from different lanes are never combined into a synthetic
percentile or a portable SLA.

| Lane               | What it establishes                                                                                                                                                           | Retention                             |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| PR CI              | Full correctness, platform packaging, focused 10k/100k cold and one-file graph budgets                                                                                        | GitHub checks and benchmark artifacts |
| Production-large   | One exact-SHA, pinned-runner observation with cold, one-file overlay, independent clean rebuild, per-phase CPU/RSS/bytes, storage, parity, and bounded Manager/query evidence | 90-day artifact                       |
| Heavy tail         | Parser/cache behavior for duplicate blobs, low-signal JSON/SVG, large TypeScript, interruption/resume, and deterministic one-versus-four-worker output                        | 90-day artifact                       |
| Focused properties | Lifecycle convergence/protection, incremental equivalence, ranking, removal idempotence, and fair workset allocation                                                          | Normal Vitest suite                   |
| Installed runtime  | Exact clean-HEAD installation and affected-flow smoke on the development host                                                                                                 | Active Threadnote handoff             |

Required PR CI
[`31354131012`](https://github.com/Kashkovsky/threadnote/actions/runs/31354131012) and PR platform evidence
[`31354131109`](https://github.com/Kashkovsky/threadnote/actions/runs/31354131109) passed on `807eda0`; PR platform
evidence [`31355055624`](https://github.com/Kashkovsky/threadnote/actions/runs/31355055624) then passed on `57fe564`.
Those runs include serialized heavy-state execution, packaged macOS structured-output coverage, ordinary lexical/recall
lanes, bytecode targets, and the resource-heavy properties. The final containing PR head must still have required CI
green before merge, and the exact run is retained on PR #131 and in the active handoff.

The first strict manual run measured `3b7c52c270702d497f01a1179f8b436cb4ac6657` and exposed four stale
harness/platform contracts corrected by `46fb831`:

- vector positive controls still required the pre-M7 documentation score instead of applying the public relevance
  policy;
- Windows completed the graph gates but briefly retained closed SQLite handles during owned temporary-directory
  cleanup;
- the heavy-tail harness expected metadata symbols/cache facts for JSON/SVG classes that H1 now excludes before
  admission; and
- MCP completion logging allowed only 50 ms on Windows, so a returned tool error could lose its privacy-safe finish
  record.

Full manual run [`31354132539`](https://github.com/Kashkovsky/threadnote/actions/runs/31354132539) passed heavy-tail and
all ordinary Linux/macOS correctness lanes on `807eda0`. It retained production-large artifact
[`9050357775`](https://github.com/Kashkovsky/threadnote/actions/runs/31354132539/artifacts/9050357775), SHA-256
`68feea91fb773d821b1524c3c66872b6da5495f4a7d270a15ac2c917cbe2bf64`, after the unchanged 20-minute measured
boundary. Scanning completed with about 927 MB peak RSS; materialization was still active at the boundary with about
741 MB peak RSS, a 4.73 GB main SQLite high-water mark, and a 352 MB WAL high-water mark. This is retained progress
telemetry, not an end-to-end performance pass; the beta makes no maximum-performance claim.

That run also exposed an evidence-only vector cursor/stat race and a hosted-Windows development-fixture result 13 ms
over its former cross-platform one-file ceiling. `57fe564` made storage telemetry tolerant of an entry atomically
disappearing between directory enumeration and stat. Focused manual run
[`31355205855`](https://github.com/Kashkovsky/threadnote/actions/runs/31355205855) then passed the 10k pinned-vector gate
and all ordinary lanes except a heavily scheduled Windows runner, whose dependency installation alone took 181 seconds
and inflated hot query, one-file indexing, and structural analysis together. The closeout uses explicit Windows-only
development-fixture headroom while retaining the tighter Linux/macOS, vector, scale, quality, and safety gates. Required
Windows packaging simultaneously exposed one omitted lifecycle entry in an eight-process production-log burst; the
final PR head gives that low-volume Windows lock queue a bounded five-second budget so completed invocations retain both
entries. Neither change extends the production-large boundary or weakens a correctness gate.

Exact-tag production-large evidence starts again automatically after `v4.1.0-beta.1` is pushed. Release publication
does not misrepresent either bounded `n=1` observation as a universal latency claim.

## Release gates

The beta is eligible only when required PR checks are green and the retained evidence reports no:

- omission of manifest-declared active source;
- repository path, source content, credential, or local-root leak;
- unbounded writer wait or abandonment of a lock-verified live owner;
- cross-worktree graph contamination or loss of the prior ready snapshot on failure;
- full-repository rewrite for a proven one-file closure;
- unsafe low-disk continuation or non-converging removed-worktree storage.

These are correctness and containment gates. “Maximum performance” means the measured profile exposes no known
dominant bottleneck that is being hidden by a timeout increase. It does not mean every repository or machine will
observe the same wall time.

## Deliberate boundaries

- The production-large artifact is `n=1` on a pinned runner. Trend claims require multiple retained runs on the same
  runner class.
- The public surrogate preserves reviewed shape and privacy-safe controls; private exact-repository evidence remains
  private and is summarized only through aggregate, path-free observations.
- Workset queries inspect existing ready snapshots only and never fan out cold builds.
- Mobile resource extraction indexes textual identifiers and explicit wiring, not pixels, visual semantics, or binary
  asset contents.
- Optional vectors remain a separate readiness phase; structural graph availability is the release-critical boundary.
